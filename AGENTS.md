# AGENTS.md

写给 AI 编码代理的项目说明。读这份文件前默认你对本项目一无所知。

## 项目概览

本仓库是一个 **Agent Skill 集合仓库（含 lesson-plan）**，不是传统软件项目——没有源码、依赖、构建或部署。它的"代码"是供 AI agent 执行的流程文档 + 模板，目标是为**企业内训/成人培训的新手讲师**一次产出一节课的两份装备：

- **教案**（`教案-<主题>.md`）：给讲师备课和自由修改，以 skill 目录内 `教案设计.md` 为模板填写；
- **课件**（`课件-<主题>.html`）：给学员看的单文件幻灯片，以 skill 目录内 `references/slide-shell.html` 为壳生成，双击即播（Reveal.js 已内联，离线可用）。

核心设计理念：只保留 6 条"条条能对应到具体教学动作"的教育学原则（成人学习、从做中学、最近发展区支架、及时反馈、组块化、主动建构），并把原则落点写进产出物本身。

**`skills/lesson-plan/SKILL.md` 是唯一权威流程**——何时使用、执行流程（7 步）、幻灯片结构与内容约束、设计经验（9 条）都在那里。本文件只是速查和护栏；改教学流程或设计原则时应改 `SKILL.md` 并同步本文件。

## 安装与分发

仓库遵循 [skills.sh](https://github.com/vercel-labs/skills) 的 skill 目录约定（`skills/<name>/SKILL.md`），用户通过 `npx skills add jiangfire/lesson-plan-skills` 安装；CLI 会把 `skills/lesson-plan/` 整个目录装入各 agent 的 skills 目录。因此 **skill 目录必须自包含**——SKILL.md 引用的模板、放映壳、素材库规则都在目录内，路径全部相对。

## 目录结构与文件角色

```
skills/lesson-plan/       # skill 自包含目录
  SKILL.md                # Skill 主文件：流程、约束、设计经验（权威）
  教案设计.md             # 教案模板（含 6 原则对照表 + 40 分钟节奏 3+2+10+12+8+5）
  references/
    slide-shell.html      # Reveal.js 放映壳（只读）
  素材库/                 # 背景资料：讲师自备 + AI 搜集（需讲师审查）
    README.md             # 素材库规则
```

具体课程的产出（`教案-*.md` / `课件-*.html`）和素材内容不入库，由 `.gitignore` 排除。

| 文件 | 角色 | 修改规则 |
| --- | --- | --- |
| `skills/lesson-plan/SKILL.md` | 流程、约束、设计经验 | 改教学流程/设计原则时改这里 |
| `skills/lesson-plan/教案设计.md` | 教案模板 | 只改结构占位，不写具体课题内容 |
| `skills/lesson-plan/references/slide-shell.html` | 放映壳：内联 Reveal.js、固定主题 CSS、播放器 JS | **只读不改**；已固化 1280×720 画布等比缩放、fragment、练习倒计时、S 键讲师提示、`?print-pdf` 打印 |
| `教案-*.md` / `课件-*.html` | 具体课程产出（不入库，.gitignore 排除） | 教案 md 是主文件，课件永远从教案重新生成 |
| `skills/lesson-plan/素材库/` | 背景资料 | AI 搜集的素材文件头必须标来源 URL + 检索日期 + "状态：待审查"，经讲师确认（改为"已审查"）才能用 |

## 技术栈与"构建"

- **技术栈**：纯 Markdown + 单文件 HTML（内联 Reveal.js 5、固定 CSS 主题和播放器 JS）。无包管理器、无配置文件（无 pyproject.toml / package.json 等）。
- **"构建"流程 = 生成流程**（详见 `SKILL.md` 执行流程）：补齐最小信息（最多集中提问 1 次；默认 40 分钟 / 企业内部成人 / 讲授+实操+提问）→ 读两个模板 → 检索素材库、不足则联网搜集并请讲师审查 → **先写教案 md** → **再从教案生成课件 html**（复制放映壳，只替换 `<!-- 幻灯片从这里开始 -->` 与 `<!-- 幻灯片到这里结束 -->` 标记区内的内容）→ 校验 → 交付。
- 课件页序直接映射教案第五节"教学过程"的环节；只用壳里定义好的 5 种版式：`layout-cover` / `layout-bullets` / `layout-cols` / `layout-task` / `layout-closing`。

## 硬约束（违反即返工）

1. **课件单文件离线**：禁止把内联的 Reveal.js 改成 CDN 引用；不引入任何外部 CSS/JS/字体/图片。
2. **课件不改壳**：只用壳里 5 种版式，只替换标记区内容；壳的内联库、主题 CSS、播放器 JS 一律不动。
3. **时间预算守恒**：各环节之和恒等于课时；新增环节必须显性扣减，禁止用"（含上）"藏时间；练:评 ≈ 2:1；其他时长（20/60/90）按同比例缩放。
4. **提问不破坏节奏**：题目随计时亮出；冷叫三步（先问全班 → 给思考时间 → 最后点名）；冷场 5 秒教师自答兜底；一次只点 1–3 人，提问时间从"评"的预算里出。
5. **"咯噔"验收**：每个组块至少 1 个反直觉点，学员第一反应应是"我怎么没想到"→"原来如此"（先意外、后通透，缺一半即不合格）；哲学概念必须贴肉、一次课 ≤3 个、每个一句话讲透；备用弹药只写进讲师提示和教案，不上屏。
6. **分步讲解用 fragment**：推导、答案讲评、先问后亮的揭晓加 `class="fragment"`；一页 ≤5 个；问题和答案禁止同时可见。
7. **内容约束**：一页一信息，标题即结论；要点 ≤6 条、每条 ≤20 字；细节话术只进教案 md 和 `<aside class="note">`；练习页必用 `layout-task` + `data-timer="分钟数"`；每个练习后必有答案/示例页（收敛型紧跟练习页，发散型放在提问分享之后）；原则编号 ①–⑥ 用 `<span class="tag">①</span>` 标注；图片只能 base64 内嵌（禁外链/本地引用），一页最多一张。
8. **不编造**：用户单位的课程事实、岗位名称、学员人数缺失时用 `＿＿＿` 占位。
9. **产出不进本仓库**：课程产出（`教案-*.md` / `课件-*.html`）和素材内容一律生成到调用者的当前工作目录，不写入本仓库；本仓库只维护 skill 本身。

## 校验与测试

没有自动化测试套件。生成/修改课件后按以下方式自检（对应 `SKILL.md` 第 6 步）：

- **结构自查**：页数 ≈ 课时（40 分钟课 15–18 页）；练习页都有 `data-timer`；答案示例页时序正确；fragment 用在分步处且一页 ≤5 个；每页有 `<aside class="note">` 讲师提示；逐条过一遍 `SKILL.md` 的 9 条"设计经验"。
- **脚本语法校验**：抽出内联 `<script>` 块跑 `node --check`（需要有 node）。
- **人工试播**：让用户双击课件验证：←/→ 翻页、fragment 逐条浮现、练习页进页自动倒计时、按 S 看讲师提示、地址栏加 `?print-pdf` 后 Ctrl+P 导出 PDF。

## 安全与合规注意事项

- **素材审查制**：AI 联网搜集的素材一律标"状态：待审查"，未经讲师确认不得用于生成；素材内容优先于模型自身知识，冲突时以素材为准，但不得整段复制进产出物（提取要点即可）。
- **事实不编造**：素材要求带来源 URL 供核查；用户单位的真实信息缺失时占位，不虚构。
- **离线单文件**：课件不得引入任何外部资源——既是运行约束，也避免把用户内容泄露给第三方 CDN/字体服务。
- 产出为一次性文件，不维护课程进度或多版本看板；读用户已有教案/附件/素材时用现有文件读取能力，不写解析脚本。

## 记忆

本项目的教学设计经验、技术方案和用户偏好已存入长期记忆（标签 `project:lesson-plan`），开始任务前先回忆。
